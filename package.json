{
  "name": "shareme-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/docs\" \"npm run dev -w apps/tools\" \"node server.js\"",
    "build": "node scripts/build.js",
    "preview": "npm run serve -w apps/docs",
    "deploy": "npm run build && npm run deploy -w apps/docs"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "concurrently": "^10.0.3",
    "express": "^5.2.1",
    "http-proxy-middleware": "^4.1.1"
  }
}
